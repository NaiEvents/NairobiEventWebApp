<%= form_for(@naievent) do |f| %>
  <% if @naievent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@naievent.errors.count, "error") %> prohibited this naievent from being saved:</h2>

      <ul>
      <% @naievent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
